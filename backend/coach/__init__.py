"""Coach engine (phase 5): descriptive currencies -> Census fit -> stat replay
-> ranked advice, plus the raid-leader report. Census is a prior, the parse is
the evidence; every recommendation scales through the fitted coefficient."""
